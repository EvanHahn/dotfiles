const express = require('express')

const PORT = 9999

const app = express()

app.use((req, res) => {
  res.send('Hello world!')
})

app.listen(PORT, () => {
  console.log('App started on port ' + PORT)
})
