#!/usr/bin/env bash
set -e
set -u

browser_open() {
  local browser="$BROWSER"
  if [ "$browser" == '' ]; then
    browser='open'
  fi
  exec "$browser" "$@"
}

if [ $# -eq 0 ]; then
  browser_open 'https://duckduckgo.com'
else
  browser_open "https://duckduckgo.com/?q=$*"
fi
