my squiggle
===========

installation:

```bash
# clone this puppy
git submodule update --init --recursive

cd script
./symlink.sh

vim +NeoBundleInstall +qall
./osx.sh # if on OSX
./debian.sh # if on Ubuntu/Debian/whatever
```

wow
