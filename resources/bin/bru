#!/usr/bin/env bash
set -e

if [[ "$(uname -s)" == 'Darwin' ]]; then
  softwareupdate -ia
fi

brew update
brew upgrade --all

gem update

npm --silent --global update

brew cleanup
brew prune
if [[ "$(uname -s)" == 'Darwin' ]]; then
  brew cask cleanup
fi
